name: 7.0-GITHUB_OUTPUT-variables
on: 
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step level output
        id: tag1
        run: | 
          echo "Application=swiggy" >> $GITHUB_OUTPUT
          echo "Environment=prod" >> $GITHUB_OUTPUT
      - name: printing above outputs.
        run:  |
          echo "Application is: ${{steps.tag1.outputs.Application}}"
          echo "Environment is: ${{steps.tag1.outputs.Environment}}"

  job2:
    needs: job1
    runs-on: ubuntu-latest
    outputs:
      Application: ${{steps.tag2.outputs.value1}}
      Environment: ${{steps.tag2.outputs.value2}}
      app_url: ${{steps.tag2.outputs.app_url}}
    steps:
      - name: job level output
        id : tag2
        run: | 
          echo "value1=Paytm" >> $GITHUB_OUTPUT
          echo "value2=test" >> $GITHUB_OUTPUT
          echo "app_url=www.paytm.com" >> $GITHUB_OUTPUT

  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - name: printing previous job outputs
        run: |
          echo "Application is: ${{needs.job2.outputs.Application}}"
          echo "Environment is: ${{needs.job2.outputs.Environment}}"
          echo "App Url is: ${{needs.job2.outputs.app_url}}"





